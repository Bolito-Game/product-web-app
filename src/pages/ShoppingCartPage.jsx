import React from 'react';

const ShoppingCartPage = () => {
  /* Header is already shown by <App>, nothing else is required here */
  return (
    <div className="shopping-cart-page">
      {/* Empty by requirement – add cart items here later */}
    </div>
  );
};

export default ShoppingCartPage;